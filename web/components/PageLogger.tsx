'use client'

import { useEffect } from 'react'

export default function PageLogger({ pageName, data }: { pageName: string; data?: Record<string, any> }) {
  // Debug logging removed to prevent Chrome Private Network Access warnings
  return null;
}

