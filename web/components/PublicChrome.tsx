'use client'

/**
 * @deprecated Use MarketingChrome (marketing routes) or AppPublicChrome (vault/fan).
 * Kept for backward compatibility with older imports.
 */
export { default } from '@/components/MarketingChrome'
