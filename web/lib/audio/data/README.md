# Sonic DNA genre intelligence (web mirror)

Source of truth: `knowledge/sonic-dna/genre-intelligence.json`

This copy is bundled for Next.js (client + API) so every report path uses the same encyclopedia as Python `compose_intelligence`.

After editing the knowledge file, sync:

```bash
cp knowledge/sonic-dna/genre-intelligence.json web/lib/audio/data/genre-intelligence.json
```
