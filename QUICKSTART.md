# Quick Start Guide

## 🚀 Getting Started

### Web App

1. **Navigate to web directory:**
   ```bash
   cd web
   ```

2. **Install dependencies:**
   ```bash
   npm install
   ```

3. **Add your images:**
   - Place gallery images in `public/images/gallery/`
   - Place release artwork in `public/images/releases/`
   - Use the filenames specified in the data JSON files

4. **Start development server:**
   ```bash
   npm run dev
   ```

5. **Open in browser:**
   - Visit http://localhost:3000

### Mobile App

1. **Navigate to mobile directory:**
   ```bash
   cd mobile
   ```

2. **Install dependencies:**
   ```bash
   npm install
   ```

3. **Start Expo:**
   ```bash
   npm start
   ```

4. **Run on device:**
   - Press `i` for iOS simulator
   - Press `a` for Android emulator
   - Scan QR code with Expo Go app on your phone

## 📝 Next Steps

1. **Add Images:**
   - Copy your gallery images to `web/public/images/gallery/`
   - Copy release artwork to `web/public/images/releases/`
   - Update image URLs in `mobile/src/data/gallery.json` if using remote hosting

2. **Customize:**
   - Update data in JSON files in `web/data/` and `mobile/src/data/`
   - Modify components in `web/components/` and `mobile/src/screens/`

3. **Deploy:**
   - Web: Deploy to Vercel, Netlify, or your preferred hosting
   - Mobile: Build with Expo for App Store/Play Store

## 🎨 Features

- ✅ Responsive web design
- ✅ Native mobile apps (iOS & Android)
- ✅ Image gallery with filtering
- ✅ Complete EPK page
- ✅ Social media integration
- ✅ Music releases showcase
- ✅ Performance history

## 📧 Support

For questions: sergikdrops@gmail.com

